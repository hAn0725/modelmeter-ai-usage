# ModelMeter — AI 用量监控

[![License: MIT](https://img.shields.io/github/license/hAn0725/modelmeter-ai-usage)](https://github.com/hAn0725/modelmeter-ai-usage/blob/main/LICENSE)

面向 VS Code AI Chat 的多模型用量、官方 API 成本与会话分析工具。

在一处掌握 GitHub Copilot 与第三方 BYOK 模型（DeepSeek、GLM、Qwen、MiMo 等）的 Token 消耗与预估费用。

> ℹ️ ModelMeter 基于 [feimacode/copilot-alternatives](https://github.com/feimacode/copilot-alternatives) 修改，为独立社区分支，并非原项目官方产品。

---

## ✨ 功能

### 📊 Token 用量统计

准确了解你每个厂商、每个模型、每天的 Token 消耗与预估费用。

- **实时统计** — 自动读取 VS Code 聊天会话存储，无需手动配置
- **人民币显示** — 所有费用按 ¥ 显示（汇率可在设置中调整）
- **丰富仪表盘** — 基于 Chart.js 的用量总览、厂商仪表盘与模型仪表盘（堆叠柱状图、甜甜圈图、趋势线）
- **日 / 周 / 月视图** — 一键切换时间范围，或自选起始日期
- **状态栏指示** — 常驻显示今日 Token 与预估费用；悬停查看 24 小时 / 7 天 / 30 天汇总
- **费用估算** — 基于本地价格表估算，未知价格模型显示“暂无价格”，不套用其它模型价格
- **数据导出** — 将用量数据导出为 JSON 供外部分析
- **AI 环境影响估算** — 根据 Token 用量估算能耗与 CO₂（启发式，仅供参考）

![用量总览](https://raw.githubusercontent.com/hAn0725/modelmeter-ai-usage/main/assets/screenshots/usage-dashboard.png)

### 📝 会话历史与分析

浏览历史聊天会话的逐轮详情。

- **会话列表** — 侧边栏展示最近会话（可一键展开全部），支持按日期或模型筛选（命令面板）
- **轮次详情** — 输入 / 输出 Token、工具调用、耗时（首次响应耗时、总耗时）、单轮输入上下文构成色条
- **会话级输入上下文构成** — 按输入 Token 加权聚合五类构成（系统指令 / 工具定义 / 对话消息 / 文件上下文 / 工具结果），覆盖不足时如实标注
- **会话仪表盘** — 轮次表格与明细（BYOK / 内置标记、费用等）
- **复制会话 ID** — 便于反馈问题或分享

![会话详情](https://raw.githubusercontent.com/hAn0725/modelmeter-ai-usage/main/assets/screenshots/session-dashboard.png)

---

## 🚀 快速开始

1. 从 VS Code Marketplace **安装扩展**
2. **打开侧边栏** — 点击活动栏的“用量监控”图标，或运行 `打开侧边栏`
3. **查看用量** — 运行 `打开用量总览` 查看 Token 消耗与预估费用
4. **浏览会话** — 展开侧边栏中的 **会话统计** 回看历史对话

> **提示**：扩展激活瞬间完成，用量数据在启动约 0.7 秒后在后台同步；未变化的会话文件不会重复解析。历史较多时首次导入在后台进行（可通过 `历史数据回溯天数` 调整）。

---

## ⚙️ 配置

所有设置位于 `modelMeter.*`：

| 设置 | 默认值 | 说明 |
|---|---|---|
| `历史数据回溯天数` | `60` | 首次加载时导入的历史天数（1–365） |
| `实时监听回溯天数` | `1` | 实时监听器启动时扫描新会话文件的天数（1–30） |
| `美元兑人民币换算汇率` | `7.2` | 将底层美元价格估算换算为人民币显示（不联网获取） |
| `日志级别` | `普通` | `普通`：仅启动/同步摘要与警告；`调试`：输出变更文件、解析耗时等详细日志 |

---

## 📋 命令

| 命令 | 说明 |
|---|---|
| `打开用量总览` | 打开主用量总览（含图表） |
| `打开厂商用量` | 打开厂商级费用与 Token 明细 |
| `打开模型用量` | 打开模型级费用与 Token 明细 |
| `打开会话详情` | 查看单个会话的轮次与耗时 |
| `导出 Token 用量数据` | 将用量数据导出为 JSON |
| `重新构建统计数据` | 从本地会话文件重建统计数据库 |
| `Token 用量诊断（输出到日志）` | 将内部状态输出到输出面板 |
| `切换会话筛选` / `清除会话筛选` | 按日期与模型筛选会话 |
| `复制会话 ID` | 复制选中会话的 ID |

---

## 📄 费用说明

费用根据本地价格表（`data/pricing.json`）与 Token 用量估算，**仅供参考**，实际费用以 API 服务商账单为准。价格表随扩展版本更新，不会联网实时获取。

---

## 🔒 隐私

以下声明均经源码验证（扩展代码不包含任何网络请求）：

- **数据本地处理** — 统计与估算全部在本地完成，数据库保存在扩展全局存储目录
- **不上传聊天内容** — 仅读取会话文件中的 Token 计数与模型元数据，从不发送到任何服务器
- **不读取 API Key** — 扩展不访问任何 API Key 或凭据
- **不上传统计数据** — 无遥测、无分析上报
- **不自动联网更新价格** — 价格表随扩展版本内置更新

---

## 📄 许可

MIT License。详见 [LICENSE](LICENSE)。
