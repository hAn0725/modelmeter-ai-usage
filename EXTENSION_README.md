# ModelMeter — AI 用量监控

[![License: MIT](https://img.shields.io/github/license/hAn0725/modelmeter-ai-usage)](https://github.com/hAn0725/modelmeter-ai-usage/blob/main/LICENSE)

面向 VS Code AI Chat 的多模型用量、官方 API 成本与会话分析工具。

在一处掌握 GitHub Copilot 与第三方 BYOK 模型（DeepSeek、GLM、Qwen、MiMo 等）的 Token 消耗与预估费用。

> ℹ️ ModelMeter 基于 [feimacode/copilot-alternatives](https://github.com/feimacode/copilot-alternatives) 修改，为独立社区分支，并非原项目官方产品。

---

## ✨ 功能

### 📊 Token 用量统计

准确了解你每个厂商、每个模型、每天的 Token 消耗与预估费用。

- **实时统计** — 自动读取 VS Code 聊天会话存储，无需手动配置
- **人民币显示** — 所有费用按 ¥ 显示（汇率可在设置中调整）
- **丰富仪表盘** — 基于 Chart.js 的用量总览、厂商仪表盘与模型仪表盘（堆叠柱状图、甜甜圈图、趋势线）
- **日 / 周 / 月视图** — 一键切换时间范围，或自选起始日期
- **状态栏指示** — 常驻显示“当前使用模型”所属账户：按量余额（如 `DeepSeek ¥38.62`）或套餐剩余额度（如 `GLM [████░░] 68%`）；未连接时悬停可直接配置
- **费用估算** — 基于本地价格表估算，未知价格模型显示“暂无价格”，不套用其它模型价格
- **数据导出** — 将用量数据导出为 JSON 供外部分析
- **AI 环境影响估算** — 根据 Token 用量估算能耗与 CO₂（启发式，仅供参考）

![用量总览](https://raw.githubusercontent.com/hAn0725/modelmeter-ai-usage/main/assets/screenshots/usage-dashboard.png)

### 🎯 官方账户额度（可选连接）

连接你的官方账户后，状态栏与侧边栏直接展示**官方余额与套餐额度**（不再依赖估算）：

- **DeepSeek** — 官方余额（含赠金 / 充值拆分）；API Key 连接
- **GLM / Z.ai** — Coding Plan 5h / 周 / MCP 月额度；API Key 连接，支持 Global 与中国大陆区域；国内站支持预付余额（套餐账号与额度并存展示，按量账号自动显示余额）
- **Qwen / 百炼** — Token Plan 滚动 5h + 周额度；浏览器登录或手动 Cookie，支持百炼与 Qwen Cloud 区域；可选绑定阿里云 AccessKey（只读权限即可）直接显示阿里云账户余额（百炼消耗的即是它）
- **MiMo / 小米** — 账户余额 + Token Plan 套餐/月窗口；浏览器登录或手动 Cookie

侧边栏“账户与套餐”区块展示每个账户的连接状态、模式与剩余额度，展开可查看 10 格进度条、重置时间以及该账户的本地 Token 与等效 API 成本。

> **口径**：官方数据仅用于余额 / 额度；本地数据用于 Token / 会话 / 等效 API 成本。Token Plan 的“等效 API 成本”按官方按量价计算，**不代表套餐实际扣款**。
>
> **账号类型说明**：套餐额度接口只对已订阅套餐（Coding Plan / Token Plan）的账号有数据；**按量计费账号**会显示中性的“未检测到套餐订阅”（并非错误，你仍可在侧边栏看到本地 Token 与等效 API 成本）。DeepSeek / MiMo 提供按量余额接口，连接后直接显示余额；GLM 国内站按量账号自动显示预付余额；千问按量账号可在（管理账户 → Qwen → 绑定阿里云账户余额）填入阿里云 AccessKey（建议在阿里云 RAM 创建一个只读用户，授予 `AliyunBSSReadOnlyAccess`）后显示阿里云账户余额。

### 📝 会话历史与分析

浏览历史聊天会话的逐轮详情。

- **会话列表** — 侧边栏展示最近会话（可一键展开全部），支持按日期或模型筛选（命令面板）
- **轮次详情** — 输入 / 输出 Token、工具调用、耗时（首次响应耗时、总耗时）、单轮输入上下文构成色条
- **会话级输入上下文构成** — 按输入 Token 加权聚合五类构成（系统指令 / 工具定义 / 对话消息 / 文件上下文 / 工具结果），覆盖不足时如实标注
- **会话仪表盘** — 轮次表格与明细（BYOK / 内置标记、费用等）
- **复制会话 ID** — 便于反馈问题或分享

![会话详情](https://raw.githubusercontent.com/hAn0725/modelmeter-ai-usage/main/assets/screenshots/session-dashboard.png)

---

## 🚀 快速开始

1. 从 VS Code Marketplace **安装扩展**
2. **打开侧边栏** — 点击活动栏的“用量监控”图标，或运行 `打开侧边栏`
3. **查看用量** — 运行 `打开用量总览` 查看 Token 消耗与预估费用
4. **浏览会话** — 展开侧边栏中的 **会话统计** 回看历史对话
5. **（可选）连接官方账户** — 在侧边栏“账户与套餐”点击“管理…”，或运行 `管理账户连接（官方额度）`，连接后状态栏即显示官方余额 / 额度

> **提示**：扩展激活瞬间完成，用量数据在启动约 0.7 秒后在后台同步，账户额度在约 0.9 秒后尝试刷新（仅当前使用账户，且受 10 分钟最短间隔限制）；未变化的会话文件不会重复解析。历史较多时首次导入在后台进行（可通过 `历史数据回溯天数` 调整）。

---

## ⚙️ 配置

所有设置位于 `modelMeter.*`：

| 设置 | 默认值 | 说明 |
|---|---|---|
| `历史数据回溯天数` | `60` | 首次加载时导入的历史天数（1–365） |
| `实时监听回溯天数` | `1` | 实时监听器启动时扫描新会话文件的天数（1–30） |
| `美元兑人民币换算汇率` | `7.2` | 将底层美元价格估算换算为人民币显示（不联网获取） |
| `日志级别` | `普通` | `普通`：仅启动/同步摘要与警告；`调试`：输出变更文件、解析耗时等详细日志 |

---

## 📋 命令

| 命令 | 说明 |
|---|---|
| `打开用量总览` | 打开主用量总览（含图表） |
| `管理账户连接（官方额度）` | 连接 / 重新连接 / 断开 DeepSeek、GLM、Qwen、MiMo 账户 |
| `打开账户与套餐` | 聚焦侧边栏的“账户与套餐”区块 |
| `打开厂商用量` | 打开厂商级费用与 Token 明细 |
| `打开模型用量` | 打开模型级费用与 Token 明细 |
| `打开会话详情` | 查看单个会话的轮次与耗时 |
| `导出 Token 用量数据` | 将用量数据导出为 JSON |
| `重新构建统计数据` | 从本地会话文件重建统计数据库 |
| `Token 用量诊断（输出到日志）` | 将内部状态输出到输出面板 |
| `切换会话筛选` / `清除会话筛选` | 按日期与模型筛选会话 |
| `复制会话 ID` | 复制选中会话的 ID |

---

## 📄 费用说明

本地“**等效 API 成本**”根据价格表（`data/pricing.json`）与本地捕获的 Token，按对应模型官方按量 API 单价换算，**仅供参考**，实际费用以 API 服务商账单为准。对于 Token Plan，该金额仅代表按量等效价值，**不代表套餐实际扣款**。价格表随扩展版本更新，不会联网实时获取。

---

## ⚠️ 已知限制

- **浏览器自动登录**需要本机安装 Chrome 或 Edge（会打开临时配置目录的浏览器窗口；失败时可改用「手动粘贴 Cookie」）
- **GLM 预付余额**仅国内站（open.bigmodel.cn）支持；全球版（z.ai）无此接口
- **GLM Coding Plan / Qwen Token Plan 的套餐窗口展示**按官方接口与参考实现适配，**尚未在真实套餐账号上真机验证**（按量计费账号不受影响）
- **Qwen 国际区（Qwen Cloud）**的 Token Plan 查询按参考实现适配，尚未真机验证；百炼（中国大陆）已真机验证
- **阿里云账户余额**是整个阿里云账户的可用余额（不限于百炼），需要在阿里云 RAM 创建只读 AccessKey

---

## 🔒 隐私

- **Token / 会话数据完全本地处理** — 统计与估算全部在本地完成，数据库保存在扩展全局存储目录
- **不上传聊天内容** — 仅读取会话文件中的 Token 计数与模型元数据，从不发送到任何服务器
- **凭据仅存本地安全存储** — 账户连接的 API Key / Cookie 只保存在 VS Code SecretStorage（系统凭据库），不写入设置、数据库、日志与导出文件；扩展不会读取其他扩展或环境中的密钥
- **官方额度请求仅在连接后发生** — 仅当你主动连接账户后，才向对应官方域名（如 api.deepseek.com / api.z.ai / open.bigmodel.cn / 百炼控制台 / home.qwencloud.com / platform.xiaomimimo.com）请求余额与额度数据；不后台轮询，仅事件触发 + 最短刷新间隔（当前账户 10 分钟）
- **不上传统计数据** — 无遥测、无分析上报；官方额度请求只发送凭据本身与固定公共参数，绝不包含聊天内容或统计
- **不自动联网更新价格** — 价格表随扩展版本内置更新
- **错误脱敏** — 官方接口错误在写入日志 / UI 前会移除凭据样式文本

---

## 📄 许可

MIT License。详见 [LICENSE](LICENSE)。
